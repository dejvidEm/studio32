/**
 * Blog: domovská sekcia (Resources) + položka v burger menu cez `/api/layout-data`.
 * Prepni na `true` aby sa všetko znova zobrazilo.
 */
export const SHOW_HOME_BLOG_TEASER_AND_NAV = false;

/**
 * Tím: sekcia na domovskej stránke. Kód ostáva, len sa sekciu dočasne nepoužíva.
 * Prepni na `true` aby sa znova zobrazila na `/`.
 */
export const SHOW_HOME_TEAM = false;
